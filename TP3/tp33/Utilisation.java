
/**
 * Décrivez votre classe Utilisation ici.
 *
 * @author (votre nom)
 * @version (un numéro de version ou une date)
 */
public class Utilisation
{
   public static void essai(final int pN, final double pD) {
       Calcul.expressions();
       Calcul.aficheMoities(pN);
       Calcul.recNeg(pD);
       Calcul.sontProches(4.0,5.0);
   }
}
